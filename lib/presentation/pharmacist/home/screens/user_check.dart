import 'package:flutter/material.dart';

class UserCheckScreen extends StatelessWidget {
  const UserCheckScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("User Check"),
    );
  }
}
